package constant;

public enum CalculatorButtonType {
    ONE, TWO, THREE,
    FOUR, FIVE, SIX,
    SEVEN, EIGHT, NINE,
    NEGATE, ZERO, PERIOD,
    CE, C, DEL,
    DIVIDE, MULTIPLY, SUBTRACT, ADD,
    CALCULATE
}
